Launch .exe.

Enter the YouTube URL, select an output folder, and specify the desired audio format.

Click Download Audio.

Progress bar and status messages will update during the download and conversion.

On completion, a popup will confirm success; closing the popup will exit the app.

If you input an unsupported audio format, an error message will appear and the app will close automatically.
